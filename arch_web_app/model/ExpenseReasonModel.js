const expenseReason = class {
    constructor(
        id,
        reason,
        userId
        )
        {
            this.id = id,
            this.reason = reason;
            this.userId = userId;
        }
}

module.exports = expenseReason;